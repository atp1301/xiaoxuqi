[build-system]
requires = []
build-backend = "setuptools.build_meta:__legacy__"

[project]
name = "harness-mvp"
version = "0.1.0"
description = "A dependency-free, scope-aware multi-agent pentest harness demo"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
